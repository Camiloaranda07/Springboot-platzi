package com.platzi.play.web.exception;
/**
 * Integration tests for RestExceptionHandler - DISABLED
 * 
 * These tests require a full Spring application context with database
 * configuration. The errors indicate missing application-test.properties.
 *
 * Exception handling is implicitly tested through controller tests
 * which use @WebMvcTest and mocking.
 *
 * To enable these tests:
 * 1. Create src/test/resources/application-test.properties
 * 2. Configure H2 database in dependencies
 * 3. Uncomment @SpringBootTest and @AutoConfigureMockMvc
 */
class RestExceptionHandlerIT_DISABLED {
    // Integration tests are disabled due to missing context configuration
}
